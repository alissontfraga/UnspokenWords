spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/unspokenwords_test
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

  flyway:
    enabled: true
    clean-disabled: true
